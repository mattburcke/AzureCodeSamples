name: manage-azure-policy-b3b5cb03
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'policies/FY23_-_Deny_Geo-replication_to_ACR_de40383f-e373-49ff-b058-c3e3c7b38a80/**'
#      - .github/workflows/manage-azure-policy-b3b5cb03.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_b3b5cb03}}
        allow-no-subscriptions: true
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          policies/FY23_-_Deny_Geo-replication_to_ACR_de40383f-e373-49ff-b058-c3e3c7b38a80/**
